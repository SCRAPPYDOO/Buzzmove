-- build spring from root catalog or import into eclipse and make it in eclipse :)
mvn clean package

-- I added some frontend in typescript/angular as well but im not good in this as in backend :)

java -jar .\Buzzmove-0.0.1-SNAPSHOT.jar

-- package buzzmove.controller.TaskController;
-- rest services at
localhost:8080/task
